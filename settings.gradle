pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven { url "https://repo.sendbird.com/public/maven" }
        mavenCentral()
    }
}
rootProject.name = "Der Projekt for Advisors"
include ':app'
